name: Test

on:
  push:
    branches: [main]
    tags:
      - "**"
  pull_request:
    branches: [main]

    
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: sleep 30
      - name: Test
        run: echo 'd'